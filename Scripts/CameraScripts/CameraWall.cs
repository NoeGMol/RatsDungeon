using UnityEngine;

public class CameraWall : MonoBehaviour
{
    /*Ray ray = new Ray();
    public Transform target;
    

    private void Update()
    {

        RaycastHit hit;
        ray.origin = transform.position;
        ray.direction = target.position; // la direccion del rayo es hacia adelante respecto a la camara
        Debug.DrawRay(ray.origin, ray.direction * 100, Color.red); // dibuja el rayo en la escena para ver si funciona
        if (Physics.Raycast(ray, out hit, 100)) // si el rayo colisiona con algo
        {
            if (hit.collider.CompareTag("Wall")) // si el objeto tiene el tag "Wall"
            {
               
            }
        }

    }

    */

}